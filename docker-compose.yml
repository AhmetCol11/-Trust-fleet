version: '3.8'

services:
  web:
    build: .
    container_name: trustfleet_web
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_APP=run.py
      - FLASK_ENV=production
    restart: always
